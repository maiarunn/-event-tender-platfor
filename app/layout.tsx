import type React from "react"
import type { Metadata } from "next"
import { Inter } from "next/font/google"
import "./globals.css"
import Header from "@/components/layout/header"
import Footer from "@/components/layout/footer"

const inter = Inter({ subsets: ["latin"] })

export const metadata: Metadata = {
  title: "EventTender - Your Perfect Event Planning Partner",
  description:
    "Submit tenders for marriages, birthdays, corporate events and get matched with verified vendors. Make your events unforgettable with EventTender.",
  keywords: "event planning, wedding planning, birthday party, corporate events, vendors, catering, decoration",
    generator: 'v0.dev'
}

export default function RootLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return (
    <html lang="en">
      <body className={inter.className}>
        <Header />
        <main>{children}</main>
        <Footer />
      </body>
    </html>
  )
}
